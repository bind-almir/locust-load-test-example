**Deploy example dotnet app to AWS Lambda**

- install Serverless framework `npm i -g serverless`
- depending on your OS execute `./build.sh` or `build.cmd` to build an app
- execute `serverless deploy`

The resulting endpoint can be used as an environment variable for load testing.